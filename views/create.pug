extends ./layouts/common.pug
block content
        h1 create

        if errors
            each error in errors
                .alert.alert-danger.container= error
        .container
            form(action="/users/create" method="POST")
                .form-group
                    label(for="name") Name
                    input#name.form-control(type="text", name="name", value=values ? value=values.name : '')
                .form-group
                    label(for="phone") Phone
                    input#phone.form-control(type="text", name="phone", value=vlues ? value=values.phone : '')
                    //- label Sdt
                    //- input(type="text", name="name", id="id")
                    //- br
                    //- br
                    button.btn.btn-primary.mt-5 Create
